/**
 * Application routes
 */
export const ROUTES = {
    HOME: "/",
    DASHBOARD: "/dashboard"
};

/**
 * API routes
 */
export const API_ROUTES = {
    DASHBOARD: "/api/dashboard",
    DASHBOARD_SETTINGS: "/api/dashboard/settings"
};
